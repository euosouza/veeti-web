import { Injectable } from "@angular/core";

@Injectable({
  providedIn: "root"
})
export class FinancialService {
  // Placeholder for aggregated financial logic (e.g. daily totals, cash flow).
  // Currently InvoiceService handles individual invoices.
}
